# 🔐 Supabase 設定
# Supabaseプロジェクトの設定値を入力してください
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_KEY=your_supabase_service_role_key_here

# 📊 アプリケーション設定
DEBUG=True
LOG_LEVEL=INFO

# 🌐 Webアプリ設定
HOST=127.0.0.1
PORT=8000

# 🔗 データソース設定
JSDA_BASE_URL=https://market.jsda.or.jp

# ⚠️  重要な注意事項:
# 1. このファイルをコピーして ".env" ファイルを作成してください
# 2. 実際の接続情報に置き換えてください  
# 3. .env ファイルは絶対にGitにコミットしないでください
# 4. チーム内で共有する場合は、セキュアな方法を使用してください

# 📝 Supabase設定の取得方法:
# 1. https://supabase.com/ にログイン
# 2. プロジェクトを選択
# 3. Settings > API で以下を確認:
#    - Project URL → SUPABASE_URL
#    - service_role (secret) → SUPABASE_KEY
#
# ⚠️  セキュリティ注意:
# - SUPABASE_KEYにはService Role Key（全権限）を使用
# - 本番環境ではCloud Run環境変数で設定
# - ローカルでは.envファイルで管理（Git管理外）

# ==================================================
# Supabase データベース接続（PostgreSQL直接接続）
# pg_dump/pg_restore用の接続情報
# ==================================================
SUPABASE_DB_HOST=db.yfravzuebsvkzjnabalj.supabase.co
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=postgres
SUPABASE_DB_PASSWORD=your_database_password_here

# 📝 取得方法:
# 1. https://supabase.com/dashboard にログイン
# 2. Settings > Database を選択
# 3. Connection string セクションで以下を確認:
#    - Host: db.xxxxxxxxx.supabase.co → SUPABASE_DB_HOST
#    - Port: 5432 → SUPABASE_DB_PORT
#    - Database: postgres → SUPABASE_DB_NAME
#    - User: postgres → SUPABASE_DB_USER
#    - Password: [取得する] → SUPABASE_DB_PASSWORD

# ==================================================
# Cloud SQL データベース接続
# ==================================================
CLOUD_SQL_HOST=your_cloud_sql_host_here
CLOUD_SQL_PORT=5432
CLOUD_SQL_DATABASE=market_analytics
CLOUD_SQL_USER=postgres
CLOUD_SQL_PASSWORD=your_cloud_sql_password_here

# 📝 注意:
# - 本番環境ではCloud Runの環境変数で設定
# - ローカルでは.envファイルに実際の値を設定